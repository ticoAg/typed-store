# Architecture Notes

本目录用于存放工作区级或跨主题的长期架构结论，例如：

- 目录边界
- 模块职责
- 协作规范
- 通用设计原则

当前工作区尚处于初始化阶段，后续可按主题逐步补充。


当前已维护的专题：

- `api-surface.md`：当前 SDK 对外 API 面与推荐使用方式。
- `publishing.md`：发布配置与 Trusted Publisher 操作清单。
